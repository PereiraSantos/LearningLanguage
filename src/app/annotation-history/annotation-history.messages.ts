/** Mensagens de feedback do usuário (evita strings mágicas repetidas). */
export const ANNOTATION_HISTORY_MESSAGES = {
    loadError: 'Não foi possível carregar o histórico de anotações.',
} as const;
